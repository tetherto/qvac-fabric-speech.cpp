# AUTO-GENERATED by .github/scripts/sync-runner-names.mjs
# Source of truth: .github/runners.yaml
# Do not edit this file by hand.

name: Runner names

on:
  workflow_call:
    outputs:
      ubuntu_2404:
        description: "Ubuntu 24.04 hosted x64"
        value: ${{ jobs.export.outputs.ubuntu_2404 }}
      macos_14:
        description: "macOS 14 hosted (Apple silicon, Paravirtual GPU)"
        value: ${{ jobs.export.outputs.macos_14 }}
      windows_2022:
        description: "Windows Server 2022 hosted x64"
        value: ${{ jobs.export.outputs.windows_2022 }}
      linux_x64_gpu:
        description: "Self-hosted NVIDIA Linux x64 GPU"
        value: ${{ jobs.export.outputs.linux_x64_gpu }}
      macos_arm64_gpu:
        description: "Self-hosted Apple Silicon GPU/Metal"
        value: ${{ jobs.export.outputs.macos_arm64_gpu }}

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      ubuntu_2404: ${{ steps.export.outputs.ubuntu_2404 }}
      macos_14: ${{ steps.export.outputs.macos_14 }}
      windows_2022: ${{ steps.export.outputs.windows_2022 }}
      linux_x64_gpu: ${{ steps.export.outputs.linux_x64_gpu }}
      macos_arm64_gpu: ${{ steps.export.outputs.macos_arm64_gpu }}
    steps:
      - name: Export runner labels
        id: export
        run: |
          echo 'ubuntu_2404=ubuntu-24.04' >> "$GITHUB_OUTPUT"
          echo 'macos_14=macos-14' >> "$GITHUB_OUTPUT"
          echo 'windows_2022=windows-2022' >> "$GITHUB_OUTPUT"
          echo 'linux_x64_gpu=qvac-ubuntu2204-x64-gpu' >> "$GITHUB_OUTPUT"
          echo 'macos_arm64_gpu=qvac-macos26-arm64-gpu' >> "$GITHUB_OUTPUT"
